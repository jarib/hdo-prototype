Holder de ord
=============

Dependencies
------------

* Ruby + Rubygems

Usage
-----

```
$ git submodule init && git submodule update
$ gem install bundler
$ bundle install
$ bundle exec rackup config.ru -p 1234
```
